bugs:
- 

check:
- [1, 2, 3].count - is it legit?

features:
- for statement
- imports
- cast semantic
- states
- arrays
- self keyword
- native functions
- call parent function (super)

tests:
- return statement
- binary operations (lexer, parser, semantic, compiler)
- unary operations
- local variables
- while statement

done:
- logical operations
- if statements
- unary operations
- local variables
- while statement
- cast
- static funcs



** Imports **

- lexer/parser part
- match import name to file
- import from folder (pass path to folder to cmd; support multiple folders)
- parse imported file (vellum or papyrus)


import Debug -> append name to current script imports list -> recursivelly go on each importName and look for their imports. Build graph

current script
      |
      |
    --------------------
    |          |       |
import1     import2     import3


For given script file:

1) Collect all imported names to set
2) Report error if self import detected
3) For each collected module collect their child modules and build graph until leaves
4) Starting from leave nodes read module file and resolve definitions (functions, properties).
4.1) Can we process modules in parallel?
5) Moving the tree up and don't resolve twice already resolved modules

- What we import? Source or pex?
-- source (c) deepseek

- What about cycles? Compile error?
-- error (c) deepseek (need to check)

- Self import?
-- papyrus doesn't complain at compile time, need to check at runtime

- Self extend?
-- papyrus gives a compile error "cannot extend ourself"